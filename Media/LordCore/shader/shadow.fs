#version 100

precision mediump float;

// out float FragColor;                              //如果C++定义了USE_COLOR0_AS_SHADOWMAP则需要这行

void main()
{

    // gl_FragDepth = gl_FragCoord.z;
    
    // FragColor = gl_FragCoord.z;                   //如果C++定义了USE_COLOR0_AS_SHADOWMAP则需要这行
}
